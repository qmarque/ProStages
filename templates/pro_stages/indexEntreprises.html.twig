{% extends 'base.html.twig' %}
{% block title %}
Entreprises
{% endblock %}

{% block contenu %}

<table class="table table-striped table-dark">
  <thead>
    <tr>
      <th scope="col">Entreprise</th>
      <th scope="col">Activité</th>
      <th scope="col">Adresse</th>
      <th scope="col">Site Web</th>
      <th></th>

      
    </tr>
  </thead>
  <tbody>

  {% for entrepriseCourante in entreprises %}

  <tr>

    <td><a href = "{{path('stagesParEntreprise', {nom:entrepriseCourante.nom})}}" style="color:white; text-decoration:none;">
      {{ entrepriseCourante.nom }}
    </a></td>
    <td>{{ entrepriseCourante.activite }}</td>
    <td>{{ entrepriseCourante.adresse }}</td>
    <td>{{ entrepriseCourante.siteWeb }}</td>
    <td>        <a class="nav-link" href="{{path('modifEntreprise',{'id':entrepriseCourante.id})}}">Modifier</a>
    </td>

  </tr>

{% endfor %}
</tbody>
</table>

{% endblock %}

